# Start neovim with Obsession

vi() {
  local editor=${EDITOR:-vim}
  if test $# -gt 0
  then
    $editor "$@"
  else
    $editor
  fi
  # elif $(git rev-parse --git-dir >/dev/null 2>&1)
  # then
  #   $editor +GitFiles!
  # else
  #   $editor .
  # fi
}

