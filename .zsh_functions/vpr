# Reviewing a pullrequest in the $EDITOR
# only works with vim or nvim
# fugitive plugin should be installed

vpr() {
  local branch="${1:-master}"
  git pull origin "$branch"
  git merge "$branch"
  "${EDITOR:-vim}" -c ":Git difftool -y remotes/origin/$branch | tabclose 1"
}
